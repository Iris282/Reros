import 'package:flutter/material.dart';

class todo_page extends StatelessWidget {
  const todo_page({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Icon(Icons.pending_actions, size: 100,),
    );
  }
}
