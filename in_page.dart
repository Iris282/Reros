import 'package:flutter/material.dart';

class in_page extends StatelessWidget {
  const in_page({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Icon(Icons.developer_board, size: 100,),
    );
  }
}
