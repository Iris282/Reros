import 'package:flutter/material.dart';

class talk_page extends StatelessWidget {
  const talk_page({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Icon(Icons.question_answer, size: 100,),
    );
  }
}
